#!/usr/bin/env node

var maker = require('../index.js');
var path = require('path');
var packageInfo = require('../package.json');

maker('climaker', packageInfo.version, path.join(__dirname, '../commands/climaker'));
